# PyInstaller (Milestone 8)

Not implemented yet.

Planned entry points:

* `packaging/admin.spec` → Admin application
* `packaging/client.spec` → Client application

Both should bundle the shared `audio_bundle.core` package, exclude development extras, and avoid pulling network-related libraries.
